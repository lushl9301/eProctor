RequestObject with ```proctorId``` and ```requestId```

Displayer run on Proctor Client Side


Server contains ```VideoServerThread``` and ```DisplayServerThread```

run MainForGrabberShow to get image from webCam
run MainForDisplayer to invigilate image